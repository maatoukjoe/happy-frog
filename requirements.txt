# Happy Frog - Python Dependencies
# For educational HID emulation using CircuitPython

# Core dependencies
click>=8.0.0
colorama>=0.4.4

# Development dependencies
pytest>=7.0.0
pytest-cov>=4.0.0
black>=22.0.0
flake8>=5.0.0

# Note: adafruit-circuitpython-hid is not included here as it's for the microcontroller
# and should be installed via CircuitPython's library manager on the device itself 